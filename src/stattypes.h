#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <sys/types.h>

char* sizeof_size_t();
void free_string(char* ptr);
char* kind_of_copy(char*);
size_t sizeof_dev_t();
size_t sizeof_ino_t();
size_t sizeof_mode_t();
size_t sizeof_nlink_t();
size_t sizeof_uid_t();
size_t sizeof_gid_t();
size_t sizeof_off_t();
size_t sizeof_blksize_t();
size_t sizeof_blkcnt_t();
size_t sizeof_time_t();
