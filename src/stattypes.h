#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <sys/types.h>

char* size_to_string(size_t);
void free_string(char* ptr);
char* kind_of_copy(char*);
char* sizeof_dev_t();
char* sizeof_ino_t();
char* sizeof_mode_t();
char* sizeof_nlink_t();
char* sizeof_uid_t();
char* sizeof_gid_t();
char* sizeof_off_t();
char* sizeof_blksize_t();
char* sizeof_blkcnt_t();
char* sizeof_time_t();
